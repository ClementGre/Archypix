{
  "db_name": "PostgreSQL",
  "query": "UPDATE tagging_services\n               SET config = jsonb_set(config, '{rules}', (\n                       SELECT COALESCE(jsonb_agg(\n                                  CASE WHEN elem->>'id' = $3::text THEN $4 ELSE elem END\n                                  ORDER BY ord), '[]'::jsonb)\n                       FROM jsonb_array_elements(config->'rules') WITH ORDINALITY AS t(elem, ord)\n                   )),\n                   updated_at = now() AT TIME ZONE 'utc'\n               WHERE id = $1 AND owner_id = $2 AND service_type = 'rule'::service_type\n                 AND EXISTS (\n                   SELECT 1 FROM jsonb_array_elements(config->'rules') e\n                   WHERE e->>'id' = $3::text)",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Uuid",
        "Uuid",
        "Text",
        "Jsonb"
      ]
    },
    "nullable": []
  },
  "hash": "e7819a1ae562add6531f2ea9ad76281ef7dac4cdc43240f581e5df727f9ccd32"
}
