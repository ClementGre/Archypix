import {PagePlaceholder} from '@/components/layout/PagePlaceholder'

export default function AdminPage() {
    return (
        <PagePlaceholder
            title="Admin"
            description="Instance health and analytics, user management, job queue, errored shares, and federated instances."
        />
    )
}
