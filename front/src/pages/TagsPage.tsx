import {PagePlaceholder} from '@/components/layout/PagePlaceholder'

export default function TagsPage() {
    return (
        <PagePlaceholder
            title="Tags"
            description="Manage your tag hierarchy: browse, rename, and bulk-organise tags across the whole library."
        />
    )
}
