import {PagePlaceholder} from '@/components/layout/PagePlaceholder'

export default function TaggingPage() {
    return (
        <PagePlaceholder
            title="Tagging pipeline"
            description="The ordered pipeline of rule, segmentation and shared-tag-mapping services that auto-assign tags. Drag to reorder; toggle and gate each service."
        />
    )
}
