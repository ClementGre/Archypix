import {PagePlaceholder} from '@/components/layout/PagePlaceholder'

export default function SettingsPage() {
    return (
        <PagePlaceholder
            title="Settings"
            description="Profile (display name, email) and library settings such as the versioning mode."
        />
    )
}
