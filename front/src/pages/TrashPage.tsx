import {PagePlaceholder} from '@/components/layout/PagePlaceholder'

export default function TrashPage() {
    return (
        <PagePlaceholder
            title="Trash"
            description="Soft-deleted photos awaiting restore or permanent removal. (The gallery can also show trashed items via Filters → Include trashed.)"
        />
    )
}
